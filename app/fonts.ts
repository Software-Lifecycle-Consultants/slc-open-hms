// Importing the fonts from the "next/font/google" package
import { Inter, Lora, Mulish, Montserrat } from "next/font/google";

/* Configures the Inter font with specified subsets and display options. */
export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

/* Configures the Lora font with specified subsets and display options. */
export const lora = Lora({
  subsets: ["latin"],
  display: "swap",
});

/* Configures the Mulish font with specified subsets and display options. */
export const mulish = Mulish({
  subsets: ["latin"],
  display: "swap",
});

/* Configures the Montserrat font with specified subsets and display options. */
export const montserrat = Montserrat({
  subsets: ["latin"],
  display: "swap",
});
